/**
 * Helpers locais (placeholder).
 *
 * As tasks 16.3 e 16.4 inicialmente criaram helpers aqui, mas apos a
 * conclusao da W5-B (que produziu `tests/setup/jwt.ts` e
 * `tests/setup/db.ts`), passamos a reutilizar esses arquivos. Mantemos
 * este modulo vazio para preservar compatibilidade caso outros testes
 * referenciem o caminho.
 */

export {};
